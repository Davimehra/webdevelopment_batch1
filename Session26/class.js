class mathmatics {
  x = 10;
  constructor() {} // use case constructor
  // bench 1 students
  // method or function
  sum(x, y) {
    return x + y;
  }

  //bench2 students
  // method or function
  divide(x, y) {
    return x / y;
  }

  //bench3 students
  // method or function
  square(x) {
    return x * x;
  }
}

let copy1 = new mathmatics(12, 45);
// encapsulation of all the methods,variables or other forms in an single class and made a new memory
// object

module.exports = copy1;
